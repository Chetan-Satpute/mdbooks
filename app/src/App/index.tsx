import React, { FC } from "react";
import Book from "../components/Book";

const App: FC = () => {
  return (
    <div className="h-screen w-screen">
      <Book />
    </div>
  );
};

export default App;
