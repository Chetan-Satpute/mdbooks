import React, { FC } from "react";
import Page from "../components/Page";

const App: FC = () => {
  return (
    <div className="h-screen w-screen">
      <Page />
    </div>
  );
};

export default App;
